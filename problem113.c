/*
 * Project Euler - Problem 113
 * Source: http://odz.sakura.ne.jp/projecteuler/index.php?cmd=read&page=Problem%20113
 * 
 * Problem 113 「非はずみ数」  †
 * ある数の桁を左から右へと順に見たとき, 任意の桁の数が自身の左にある桁の数以上であるとき, その数を増加数 (increasing number)
 * と呼ぶ; 例えば134468は増加数である.
 * 同様に, 任意の桁の数が自身の右にある桁の数以上であるとき, その数を減少数 (decreasing number) と呼ぶ;
 * 例えば66420がそうである.
 * 増加数でも減少数でもない正の整数を "はずみ"数 ("bouncy" number) と呼ぶ; 155349がそうである.
 * nが大きくなるにつれ, n以下のはずみ数の割合は大きくなる. 例えば, 100万未満では, はずみ数でない数は12951個しかない. 同様に,
 * 1010未満では277032個しかない.
 * googol数 (10100) 未満ではずみ数でないものの数を答えよ.
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <stdbool.h>

int main() {
    printf("Project Euler - Problem 113\n");
    
    // TODO: Implement solution here
    
    return 0;
}
