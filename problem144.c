/*
 * Project Euler - Problem 144
 * Source: http://odz.sakura.ne.jp/projecteuler/index.php?cmd=read&page=Problem%20144
 * 
 * Problem 144 「レーザービームの多重反射の調べ上げ」  †
 * レーザー物理学では, "white cell" とはレーザー光線を遅延させるための鏡の装置のことである. 光線は cell に入り,
 * 鏡で反射して, 最終的には飛び出す.
 * 特定の white cell は, 方程式 4x2 + y2 = 100 で表される楕円と考えることができる.
 * −0.01 ≤ x ≤ +0.01 に対応する上部に穴が空いていて, 光線が出入りできるようになっている.
 * この問題では, 光線は white cell の外側 (0.0, 10.1) から始まり, 最初に (1.4, -9.6) で鏡に反射するものとする.
 * 光線は, 楕円の表面に当たるごとに, 反射の法則に従う. つまり, 入射する光線と反射する光線は入射する点の法線に対して同じ角度をなす.
 * 上の左の図では, white cell と赤線で光線の最初に反射する 2 点を, 青線で最初に反射する点の接線を示している.
 * 与えられた楕円に対する点 (x, y) での接線の傾き m は, m = -4x/y となる.
 * 法線とは, 反射する点での接線に垂直な線である.
 * 右側の animation は光線の最初の 10 回の反射を示している.
 * 光線は white cell から出るまでに何回反射するか?
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <stdbool.h>

int main() {
    printf("Project Euler - Problem 144\n");
    
    // TODO: Implement solution here
    
    return 0;
}
