/*
 * Project Euler - Problem 364
 * Source: http://odz.sakura.ne.jp/projecteuler/index.php?cmd=read&page=Problem%20364
 * 
 * Problem 364 「気楽な距離」  †
 * N 個の一列に並んだ座席がある. N 人の人たちが以下のルールに従って次々に座席を埋めていく.
 * 隣接する座席が空いている座席があれば, その座席を取る.
 * そのような座席がなく, 隣接する座席が一つのみ埋まっている座席があれば, その座席を取る.
 * それ以外の場合は, 残っている利用可能な座席を取る.
 * このルールで N 人の人たちが N 個の座席を取るときの可能性の数を T(N) としよう.
 * 以下の図により T(4)=8 であることがわかる.
 * #ref(): File not found: "p_364_comf_dist.gif" at page "Problem 364"
 * T(10) = 61632, そして T(1 000) mod 100 000 007 = 47255094 となることが確かめられる.
 * T(1 000 000) mod 100 000 007 を求めよ.
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <stdbool.h>

int main() {
    printf("Project Euler - Problem 364\n");
    
    // TODO: Implement solution here
    
    return 0;
}
