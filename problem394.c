/*
 * Project Euler - Problem 394
 * Source: http://odz.sakura.ne.jp/projecteuler/index.php?cmd=read&page=Problem%20394
 * 
 * Problem 394 「パイ食べ問題」  †
 * ジェフは一風変わった方法でパイを食べる.
 * パイは円形状である. 彼は半径にそってパイに最初の切り込みを入れる.
 * そして残りのパイが少なくとも与えられた割合 F 以上ある場合, 彼は以下の手順を行う.
 * パイの中心から残りのパイの縁の任意の点まで切り込みを入れ, 再び同じ確率で, 残りのパイの縁の任意の点まで切り込みを入れる.
 * これで残りのパイは3つに分けられる.
 * 最初の切り込みから反時計回りで, 最初の2切れのパイを取って食べる.
 * パイの残りが割合 F 未満になった時, 彼はこの手順をやめる. かわりに残りのパイをすべて食べる.
 * x ≥ 1 に対し, F = 1/x のときジェフが手順を繰り返す回数の期待値を E(x) としよう.
 * E(1) = 1, E(2) ≈ 1.2676536759, そして E(7.5) ≈ 2.1215732071 であることが確かめられている.
 * E(40) を求め, 小数点以下11桁の位で四捨五入して答えよ.
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <stdbool.h>

int main() {
    printf("Project Euler - Problem 394\n");
    
    // TODO: Implement solution here
    
    return 0;
}
