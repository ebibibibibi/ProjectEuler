/*
 * Project Euler - Problem 545
 * Source: http://odz.sakura.ne.jp/projecteuler/index.php?cmd=read&page=Problem%20545
 * 
 * Problem 545 「ファウルハーバーの公式」  †
 * 1 から n までの正整数の k 乗の和はファウルハーバーの公式という有理係数を持つ k+1 次の多項式で表せる.
 * &ref(): File not found: "p545_eq1.png" at page "Problem 545";,
 * ここで a&sub{i}; は既約分数 p&sub{i}; / q&sub{i}; として書き表せる有理係数である(a&sub{i}; = 0
 * の場合, q&sub{i}; = 1 とみなす).
 * 例えば, &ref(): File not found: "p545_eq2.png" at page "Problem 545";.
 * k 乗の和に対する q&sub{1}; の値を D(k) を定義する(すなわち既約分数 a&sub{1}; の分母).
 * k ≥ 1 において D(k) = 20010 となる m 番目の k の値を F(m) と定義する.
 * D(4) = 30 (a&sub{1}; = -1/30 となるため), D(308) = 20010, F(1) = 308, F(10) =
 * 96404 が与えられている.
 * F(10&sup{5};) を求めよ.
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <stdbool.h>

int main() {
    printf("Project Euler - Problem 545\n");
    
    // TODO: Implement solution here
    
    return 0;
}
