/*
 * Project Euler - Problem 143
 * Source: http://odz.sakura.ne.jp/projecteuler/index.php?cmd=read&page=Problem%20143
 * 
 * Problem 143 「三角形のトリチェリ点の調べ上げ」  †
 * ABCを全ての内角が120度未満の三角形とする. 三角形の内側の点Xに対して, XA = p, XC = q, XB = rとする.
 * フェルマーは「p+q+rを最小にするXを求めよ」とトリチェリに問題を出した.
 * トリチェリは， 三角形ABCの各辺に正三角形 AOB, BNC, AMC を作ると, AOB, BNC,
 * AMCに外接する3つの円が三角形の内部の1点 T で交わることを示した. さらに, トリチェリ-フェルマー点と呼ばれる点 T が, p + q +
 * r を最小化することも示した. 更には, 和が最小となるときには, AN = BM = CO = p + q + r であり, AN, BM,
 * COも点Tで交わることも示せる.
 * 和が最小化されているとして, a, b, c, p, q, r が全て正の整数であるとき, 三角形 ABC をトリチェリ三角形と呼ぶ. 例えば,
 * a = 399, b = 455, c = 511 は p + q + r = 784 のトリチェリ三角形である.
 * トリチェリ三角形について 異なる値をとる p + q + r ≤ 120000 の総和を求めよ.
 * 注: この問題は最近変更された. 正しいパラメータを用いているかどうかチェックしてほしい.
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <stdbool.h>

int main() {
    printf("Project Euler - Problem 143\n");
    
    // TODO: Implement solution here
    
    return 0;
}
