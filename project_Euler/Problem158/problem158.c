/*
 * Project Euler - Problem 158
 * Source: http://odz.sakura.ne.jp/projecteuler/index.php?cmd=read&page=Problem%20158
 * 
 * Problem 158 「左隣の文字より辞書順で後になる文字がちょうど1個となる文字列の探査」  †
 * 26 文字のアルファベットから 3 個の異なった文字を取ると, 長さ 3 の文字列を作ることができる.
 * 例えば, 'abc', 'hat', 'zyx' となる.
 * この 3 つの例について調べると, 'abc' は左隣の文字より辞書順で後になる文字が 2 個ある.
 * 'hat' では, 左隣の文字より辞書順で後になる文字がちょうど 1 個となり, 'zyx' では 0 個となる.
 * 左隣の文字より辞書順で後になる文字がちょうど 1 個となるような長さ 3 の文字列は全部で 10400 個存在する.
 * いま, n ≤ 26 個の異なったアルファベットからなる文字列について考える.
 * n について, p(n) を左隣の文字より辞書順で後になる文字がちょうど 1 個となるような長さ n の文字列の個数であるとする.
 * p(n) の最大値を求めよ.
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <stdbool.h>

int main() {
    printf("Project Euler - Problem 158\n");
    
    // TODO: Implement solution here
    
    return 0;
}
