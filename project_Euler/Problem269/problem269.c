/*
 * Project Euler - Problem 269
 * Source: http://odz.sakura.ne.jp/projecteuler/index.php?cmd=read&page=Problem%20269
 * 
 * Problem 269 「少なくとも1つの整数の根を持つ多項式 」  †
 * 多項式 P(x) の根(root)または零点(zero)とは等式 P(x)=0 の解のことである.
 * Pn を n の各桁が係数となるような多項式と定義する.
 * 例えば, P5703(x)=5x3+7x+3である.
 * 以下のことがわかる:
 * Pn(0) は n の最後の桁であり,
 * Pn(1) は n の各桁の合計であり,
 * Pn(10) は n そのものである.
 * Z(k) を, 多項式 Pn が少なくとも1つの整数の根を持つような, k を超えない正の整数 n の個数とする.
 * Z(100 000) は 14696 であることが確かめられる.
 * Z(1016) を求めよ．
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <stdbool.h>

int main() {
    printf("Project Euler - Problem 269\n");
    
    // TODO: Implement solution here
    
    return 0;
}
