/*
 * Project Euler - Problem 299
 * Source: http://odz.sakura.ne.jp/projecteuler/index.php?cmd=read&page=Problem%20299
 * 
 * Problem 299 「3つの相似三角形」  †
 * 整数の座標をもつ４点が選ばれる:
 * A(a, 0), B(b, 0), C(0, c), D(0, d) (0 < a < b かつ 0 < c < d) である.
 * 線 AC 上に, 整数の座標をもつ点 P を, ３つの三角形 ABP, CDP, BDP が全て相似となるように選ぶ.
 * a=c のときにのみ３つの三角形が相似となり得ることは, 容易に証明できる.
 * そこで, a=c とし, ３つの三角形 ABP, CDP, BDP が全て相似となる点 P (整数の座標をもつ)
 * が少なくとも１つ存在するような３数の組 (a,b,d) を探すことにする.
 * 例えば, (a,b,d)=(2,3,4) ならば, 点 P(1,1) が上の条件を満たすことが容易に確かめられる. ３数の組 (2,3,4) と
 * (2,4,3) は, 点 P(1,1) は両者で共通ではあるが, 異なるものとしてみなすことに注意.
 * b+d < 100 の場合, 点 P が存在する異なる３数の組 (a,b,d) は 92 個ある.
 * b+d < 100,000 の場合, 点 P が存在する異なる３数の組 (a,b,d) は 320471 個ある.
 * b+d < 100,000,000 の場合, 点 P が存在する異なる３数の組 (a,b,d) は何個あるか.
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <stdbool.h>

int main() {
    printf("Project Euler - Problem 299\n");
    
    // TODO: Implement solution here
    
    return 0;
}
