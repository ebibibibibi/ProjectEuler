/*
 * Project Euler - Problem 557
 * Source: http://odz.sakura.ne.jp/projecteuler/index.php?cmd=read&page=Problem%20557
 * 
 * Problem 557 「三角形の分割」  †
 * 三角形は, 下図のように2つの直線で3つの三角形と1つの四角形にに分割することができます。それを a, b, c, d とします。
 * &ref(): File not found: "p557-triangle[1].gif" at page "Problem 557";
 * 合計の面積が 55 で, b ≦ c のとき, (a, b, c, d) = (22, 8, 11, 14), (20, 2, 24, 9)
 * が条件を満たします。
 * S(n) = (条件を満たす a, b, c, d の組からできる三角形の面積の合計) (a + b + c + d ≦ n) とします。
 * そのとき, S(20) = 259 です。
 * S(10000) を求めなさい。
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <stdbool.h>

int main() {
    printf("Project Euler - Problem 557\n");
    
    // TODO: Implement solution here
    
    return 0;
}
